(* Assert: complex boolean expression *)
(* Expected: ACCEPT - assert with compound boolean *)

let validate x y z =
  assert (x > 0 && y > x && z >= y);
  x + y + z

let _ = print_int (validate 1 2 3)
